import { PageHeader } from '@/components/page-header'

export default function PlatformsPage() {
  return (
    <PageHeader
      title="Platforms"
      subtitle="Platform-specific strategy, formats, constraints, and posting approach."
    />
  )
}
