# Session log — 2026-04-01 — M2 start: VEC-01, KG-01/02, INP-03 brief
Session ID: 2026-04-01-m2-start-q8v

## What we worked on
- VEC-01: Vector store setup
- KG-01 + KG-02: Graph write API (combined)
- INP-03: Input processing pipeline — brief + layout spec (build not yet started)

## What was done

**VEC-01 — Vector store setup:**
- Brief written and approved: `01-design/briefs/VEC-01-vector-store-setup.md`
- Installed `@ai-sdk/openai`
- Created `lib/db/schema/types.ts` — pgvector `customType` helper for Drizzle
- Added `embedding vector(1536)` column + HNSW index to 10 tables: `dna_business_overview`, `dna_brand_meaning`, `dna_value_proposition`, `dna_knowledge_assets`, `dna_tov_samples`, `src_source_documents`, `src_statistics`, `src_testimonials`, `src_stories`, `src_own_research`
- Generated and applied migration 0007 (`0007_pink_skaar.sql`) via Neon MCP — all 10 columns + 10 HNSW indexes verified live
- Created `lib/llm/embeddings.ts` — `generateEmbedding(text)` using OpenAI `text-embedding-3-small` (1536 dims)
- Created `lib/retrieval/similarity.ts` — `findSimilar(embedding, table, options)` cosine distance helper
- Created `lib/retrieval/index.ts` — re-exports
- Updated `lib/llm/index.ts` to export `generateEmbedding`
- Added `OPENAI_API_KEY` placeholder to `.env.local`
- Status: **done**

**KG-01 + KG-02 — Graph write API:**
- Confirmed KG-01 was not previously built (only client + seed scripts existed, no write API)
- Brief written and approved: `01-design/briefs/KG-01-02-graph-write-api.md`
- Created `lib/graph/types.ts` — `NodeLabel` + `RelationshipType` union types (all 14 labels, 22 relationship types from ADR-002), `NodeInput`, `EdgeInput`, `WriteResult`, `BatchWriteResult` interfaces. `description` required at compile time.
- Created `lib/graph/canonical.ts` — `resolveCanonical()`, `registerCanonical()`, `deriveDedupeKey()`. Auto-registration for Person/Organisation/Project only.
- Created `lib/graph/write.ts` — `writeNode()`, `writeEdge()`, `writeBatch()`. MERGE throughout, dual-write to Neon mirror, mirror failures non-fatal (logged, FalkorDB authoritative).
- Created `lib/graph/index.ts` — re-exports
- Read/traversal/search functions explicitly deferred — noted in backlog as KG-02b
- Status: **done** (write API)

**INP-03 — Input processing pipeline:**
- Brief written and approved: `01-design/briefs/INP-03-input-processing-pipeline.md`
- Layout spec written and approved: `01-design/wireframes/INP-03-layout.md`
- Establishes new `input-process` UI pattern (no template match)
- **Build not yet started** — session ended here

## Decisions made

- **VEC-01:** OpenAI `text-embedding-3-small` chosen — cheapest ($0.02/1M tokens), 1536 dims, well-tested with pgvector HNSW. `graph_nodes.embedding` deferred to KG-02b.
- **KG-01/02 combined:** FalkorDB is schema-less — "schema" is enforced by TypeScript union types, not a DB migration. Write API only in this pass; read/traversal deferred until there's real data to query against.
- **INP-03 scope:** Plain text only for v1. Two-phase extract/commit — nothing written until user confirms. 7 extraction types: Ideas, Concepts, People, Organisations, Stories, Techniques, Content Angles. Action items excluded. Statistics deferred.
- **INP-03 route:** `/dashboard/inputs/process` — logical parent section for when INP-07 queue arrives.
- **INP-03 UI:** Confirm-before-save (data quality over convenience). Results panel slides in from right, compressing main area. Confidence badges (HIGH/MED/LOW). Live count on save CTA.

## What came up that wasn't planned

- KG-01 backlog status was `planned` but the FalkorDB client and seed scripts were already built — the write API was the missing piece, not a fresh start.
- Real coaching transcripts already exist in `04-documentation/reference/coaching_transcripts/` — useful reference for INP-03 extraction design.

## Backlog status changes

- VEC-01: `planned` → `done`
- KG-01: `planned` → `done`
- KG-02: `planned` → `done` (write API) — read/traversal flagged as KG-02b
- INP-03: `planned` → `in-progress` (brief + layout approved, build pending)

## What's next

1. **INP-03 build** — start fresh session. Brief and layout both approved, build plan needed before code.
2. **OPENAI_API_KEY** — must be set in `.env.local` and Vercel before embeddings work. Not yet done.
3. **Carry-forward from previous sessions still open:**
   - `brand_users.userId → users.id` FK migration (small, can slot in any time)

## Context for future sessions

- **INP-03 state:** Brief at `01-design/briefs/INP-03-input-processing-pipeline.md` (approved), layout at `01-design/wireframes/INP-03-layout.md` (approved). Next step is the build plan (feature-build Step B).
- **INP-03 architecture:** Two functions — `extractFromText()` in `lib/processing/extract.ts` (LLM call, returns ExtractionResult, nothing written), `commitExtraction()` in `lib/processing/commit.ts` (writes on confirmation). API routes at `POST /api/process` and `POST /api/process/commit`. Side panel UI at `/dashboard/inputs/process`.
- **VEC-01 outstanding:** `OPENAI_API_KEY` not yet set — embeddings will fail until this is added to `.env.local` and Vercel.
- **KG-02b:** Graph read/traversal/search functions explicitly deferred. Build when INP-03 has populated real data.
- **M2 progress:** VEC-01 ✓, KG-01/02 ✓, INP-03 in progress. After INP-03: INP-01 (Krisp ingestion).
