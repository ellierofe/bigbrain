# Session log — 2026-04-08 — INP-03 debugging
Session ID: 2026-04-08-inp03-debug-r5q

## What we worked on
- INP-03: Input processing pipeline — end-to-end debugging through to first clean commit

## What was done

**FalkorDB fixes (`lib/graph/write.ts`):**
- Replaced `ON CREATE SET n.createdAt = timestamp()` (not supported by FalkorDB) with a separate query: `MATCH (n:Label { id: $id }) WHERE n.createdAt IS NULL SET n.createdAt = timestamp()`
- Replaced dynamic property key syntax `SET n[$key] = $value` (bracket notation not supported) with a single serialised JSON field: `SET n.properties = $properties` where `properties = JSON.stringify(input.properties)`

**LLM + embedding provider switch (`lib/llm/`):**
- `lib/llm/client.ts`: fixed env var `GOOGLE_API_KEY` → `GOOGLE_GENERATIVE_AI_API_KEY`; updated model `gemini-2.0-flash` → `gemini-2.5-flash` (2.0 deprecated)
- `lib/llm/embeddings.ts`: switched from OpenAI `text-embedding-3-small` to Gemini Embedding 2 Preview (`gemini-embedding-2-preview`, `outputDimensionality: 1536`) due to OpenAI quota exhaustion. Added post-generation truncation to 1536 dims (`embedding.slice(0, 1536)`) because `outputDimensionality` param was not being respected by the model (returned 3072).
- `.env.local`: renamed `GEMINI_API_KEY` → `GOOGLE_GENERATIVE_AI_API_KEY`

**Next.js config (`next.config.ts`):**
- Added `serverExternalPackages: ["falkordb", "@js-temporal/polyfill"]` — FalkorDB's `@js-temporal/polyfill` dep crashes in Next.js server runtime without this

**Auth bypass for dev (`middleware.ts` + route handlers):**
- Added `if (process.env.NODE_ENV === 'development') return NextResponse.next()` to middleware
- Added equivalent dev bypass in both `/api/process` and `/api/process/commit` route handlers

**Embedding writes (`lib/processing/commit.ts`):**
- Multiple iterations to fix pgvector writes — Drizzle's custom vector type serialisation issues
- Final working approach: `neon` tagged template literal directly (`import { neon }`) with `${vec}::vector` cast, where `vec = '[v1,v2,...]'` string
- Truncated Gemini embeddings to 1536 dims in `generateEmbedding()` after discovering model ignores `outputDimensionality`

**Extraction quality improvements (`lib/llm/system-prompts.ts`):**
- Added dedup instruction: "Deduplicate within each type — if two items express the same idea, concept, or technique, merge them into the stronger one"

**UI fixes (`app/(dashboard)/inputs/process/`):**
- `category-section.tsx`: added `peer` to `STORY_SUBJECTS`; increased item truncation 60→100 chars; added `getSourceQuote()` helper showing source quote as italic secondary line per item; fixed `Checkbox` — removed `'indeterminate'` (base-ui doesn't support it)
- Extraction timing instrumentation added to `lib/processing/extract.ts`: `console.log('[extractFromText] LLM call took Xms')`

**Backlog additions:**
- INP-08: Krisp meeting auto-classification
- INP-09: Context-aware extraction
- UX-05: Input processing panel review UX (edit items, not just select)
- Enhancement notes added to INP-03 entry (A–E)

## Decisions made

- **session-log skill updated** — added edge case for sessions that continue after a context reset. New log references the prior session ID and only covers work done in the current conversation. See `03-skills/session-log/SKILL.md`.


- **Gemini for both LLM and embeddings** — OpenAI quota exceeded, Anthropic credit balance empty (Claude Max subscription ≠ API credits). Gemini 2.5 Flash for extraction, Gemini Embedding 2 Preview for embeddings. Note: revert to Claude when API credits are topped up.
- **Embedding truncation over column resize** — Rather than migrating pgvector columns to 3072 dims, truncate Gemini output to 1536. Valid because Gemini Embedding 2 Preview uses Matryoshka Representation Learning — first N dimensions are a quality-preserving subembedding.
- **FalkorDB properties as JSON string** — FalkorDB doesn't support dynamic property key assignment (`SET n[$key] = $value`), so label-specific properties are stored serialised as `n.properties` (a JSON string). Readable via `JSON.parse` in retrieval layer.
- **neon tagged template for vector writes** — Drizzle's custom type serialiser was bypassed by the `as unknown as null` cast workaround; `sql.raw()` hit potential size limits; `neon` tagged template with `::vector` cast was the approach that worked reliably.

## What came up that wasn't planned

- FalkorDB turns off weekly — needs manual restart via FalkorDB Cloud dashboard
- OpenAI quota exceeded mid-session — forced full provider switch to Gemini
- Anthropic API credits separate from Claude Max subscription — need to be topped up separately for production use
- No "Saved successfully" message currently shown — commit result state exists but success toast/message not wired up (logged as UX-05 adjacent concern)

## Backlog status changes
- INP-03: `done` (confirmed working end-to-end — extraction + commit + embeddings all clean)
- INP-08: new → `planned`
- INP-09: new → `planned`
- UX-05: new → `planned`

## What's next
1. Wire up success message / completion state in the process UI (currently no feedback on clean commit)
2. INP-01: Krisp transcript ingestion — next M2 feature
3. Add `GOOGLE_GENERATIVE_AI_API_KEY` to Vercel env vars (currently only in `.env.local`)
4. `brand_users.userId → users.id` FK migration — carry-forward, still outstanding
5. Consider topping up Anthropic API credits to revert extraction back to Claude

## Context for future sessions
- FalkorDB auto-shuts off weekly — check it's running before any graph write tests
- OpenAI key in `.env.local` is over quota — don't rely on it; Gemini is the active provider
- `outputDimensionality` param on Gemini Embedding 2 Preview doesn't work — truncation happens post-generation in `generateEmbedding()` via `.slice(0, 1536)`
- FalkorDB `n.properties` field is a JSON string (not native properties) — retrieval layer must `JSON.parse` it
- Dev auth bypass is active in middleware + both process route handlers — remove before production
