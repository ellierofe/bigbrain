# Session log — 2026-04-09/10/11 — INP-01 schema + ingest skill
Session ID: 2026-04-09-inp01-schema-k3w

## What we worked on
- INP-01: Krisp transcript ingestion — Krisp MCP live test, schema-to-DB, migration, ingest skill

## What was done

**Krisp MCP live test:**
- Called `search_meetings` (last 7 days) — confirmed response shape: `meeting_id` (32-char hex, no dashes), `date` (ISO 8601 with TZ offset), `attendees` (names/usernames, no emails), `is_recurring`, `transcript.status`
- Called `get_multiple_documents` on one meeting — confirmed transcript format: markdown, `**Speaker | MM:SS**\nText` lines
- Key finding: **no participant emails available from Krisp MCP at all** — attendees are display names or usernames (e.g. `justyna.bartosik5`). Email-based dedup from the brief is not feasible; name-only fuzzy matching is the only path.

**Schema-to-DB (SKL-06):**
- Three new tables + one column addition, all generated and reviewed:
  - `02-app/lib/db/schema/inputs/contacts.ts`
  - `02-app/lib/db/schema/inputs/meeting-types.ts`
  - `02-app/lib/db/schema/inputs/pending-inputs.ts`
  - `02-app/lib/db/schema/inputs/index.ts`
  - `src_source_documents`: added `krisp_meeting_id` text column + unique index
- `02-app/lib/db/schema/index.ts`: added `export * from "./inputs"`
- Migration generated: `02-app/lib/db/migrations/0009_chubby_kabuki.sql`
- Migration manually patched to add: `CREATE EXTENSION IF NOT EXISTS pg_trgm` + `CREATE INDEX contacts_name_trgm_idx USING gin (name gin_trgm_ops)` — Drizzle can't express `gin_trgm_ops` natively
- Note: migration 0008 (`0008_medical_ulik.sql` — `ALTER TABLE src_source_documents ALTER COLUMN file_url DROP NOT NULL`) was also pending; applied both

**DB migration (SKL-09):**
- Applied 0008 and 0009 to Neon (`damp-boat-57321258`) via MCP
- All tables, columns, FKs, and indexes verified — including GIN trgm index on `contacts.name`

**Ingest skill (SKL-10 — krisp-ingest):**
- `.claude/skills/krisp-ingest/SKILL.md` written
- Supports daily mode (since last run) and `backfill N` mode
- Contact resolution: exact name match → pg_trgm fuzzy (≥0.8 match, 0.6–0.8 flag) → create new
- Skips self (`Ellie Rofe`) as a contact
- Extracts one meeting at a time (rate limit safety)
- Writes to `pending_inputs` with `ON CONFLICT DO NOTHING`

**Other:**
- `DEFAULT_BRAND_ID=ea444c72-d332-4765-afd5-8dda97f5cf6f` added to `02-app/.env.local`
- INP-01 backlog entry updated to reflect local skill trigger + no-email dedup reality
- `/schedule` skill attempted twice — remote connectivity issue both times, deferred

## Decisions made
- **No Vercel cron for Krisp ingestion** — Krisp is OAuth-only; no API key available. Token lives in Claude Code's internal session, not accessible to Vercel runtime. Solution: Claude Code skill run locally, scheduled via `/schedule` when remote connectivity is stable.
- **Email dedup not available** — Krisp MCP returns names/usernames only, no emails. Brief's email-first dedup path is moot for Krisp data. pg_trgm name fuzzy search is the only dedup mechanism.
- **GIN trgm index added manually to migration** — Drizzle limitation; noted in migration file with a comment. Future migrations on `contacts` won't disturb this index as it's not tracked in Drizzle's schema snapshot.

## What came up that wasn't planned
- Migration 0008 was pending alongside 0009 — applied both in sequence
- OAuth token extraction attempted — not accessible from filesystem; confirmed Claude Code manages it internally

## Backlog status changes
- INP-01: `in-progress` (no change — schema + skill done, pending test)

## What's next
1. **Run backfill** — `/krisp-ingest backfill 10` in a new session (Krisp MCP available). Test first batch, check `pending_inputs` table for results.
2. **INP-07: Queue UI** — build after backfill confirms data shape. Start with `/feature-brief` for INP-07, or go straight to `/feature-build` if brief is already sufficient from INP-01 brief's scope note.
3. **Retry `/schedule`** — set up daily ingest trigger once remote connectivity is stable.
4. **`meeting_types` seed data** — insert at least one row (e.g. "State Change Mastermind") so the pattern matching works during backfill. Can be done via Neon MCP in the ingest session.

## Context for future sessions
- Krisp MCP response shape confirmed: `meeting_id` (32-char hex), `date` (ISO 8601 + TZ offset), `attendees` (name/username strings, no emails), `transcript.status = "uploaded"` when ready
- `get_multiple_documents` returns full transcript as markdown — parse `**Speaker | MM:SS**\n` lines for transcript text
- No emails from Krisp — contact dedup is name-only throughout
- GIN trgm index (`contacts_name_trgm_idx`) exists in DB but not in Drizzle schema file — don't let Drizzle try to drop/recreate it
- 0008 + 0009 both applied and recorded in `drizzle.__drizzle_migrations`
- Dev auth bypass still active in middleware + process routes — remove before production
